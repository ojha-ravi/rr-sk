const expect = require("expect");

const addCounter = (list) => {
	list.push()
}

const testAddCounter = () => {
	const listBefore = [];
	const listAfter = [0];

	expect(
		addCounter(listBefore)
	).toEqual(listAfter);
}
